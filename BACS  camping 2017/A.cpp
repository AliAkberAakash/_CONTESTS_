#include<bits/stdc++.h>

using namespace std;

int m=0;

void maxo(int n)
{
    if(n>m)
        m=n;
}

int main()
{

    int t,no, c,x;

    scanf("%d",&t);

    while(t--)
    {
        scanf("%d", &no);

        for(int i=0; i<no; i++)
        {
           scanf("")
        }
    }

}
